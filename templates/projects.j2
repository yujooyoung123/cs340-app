<html>
<head>
<title>Projects</title>
<link href="style.css" rel="stylesheet" type="text/css" />
</head>

<body>
<h1>Projects</h1>

    [<a href="/">Home</a> | <a href="projects">Projects</a> | <a href="municipalities">Municipalities</a> |
     <a href="owners">Owners</a> | <a href="cases">Cases</a> |
     <a href="lawyers">Lawyers</a> ]
<div id="browse" style="display: block">

<p><strong>Browse Projects</strong></p>

<table>
  <thead>
  <table border="1" cellpadding="5">
  <tr>
      <!-- Iterate through each key in the first entry to get the
      column name -->
      {% for key in Data[0].keys() %}

      <!-- Create a <th> tag with the key inside of it, this will be
      our header row -->
      <th>{{ key }}</th>

      <!-- End of this for loop -->
      {% endfor %}
      <td>  </td>
      <td>  </td>
  </tr>
  <tbody>

  <!-- Now, iterate through every person in Projects -->
  {% for project in Data %}
  <tr>
      <!-- Then iterate through every key in the current person dictionary -->
      {% for key in project.keys() %}
      <!-- Create a <td> element with the value of that key in it -->
      <td>{{project[key]}}</td>
      {% endfor %}
      <td><a href="update-project/{{ project["projectID"] }}">Update</a></td>
      <td><a href="delete-project/{{ project["projectID"] }}">Delete</a></td>
  </tr>
  {% endfor %}
</table>

<p>&nbsp;</p>
</div> <!-- browse -->

<!-- <div>
  <a>Search for a Project </a>
  <input type="text" placeholder="Enter Project ID">
</div> -->

<div id="insert" style="display: block">
    <form method="POST" action="add-project">
        <legend><strong>Add Project</strong></legend>
          <fieldset class="fields">
            <label> Project ID </label> <input type="number" name="projectID" required>
            <label> Plat Map </label> <input type="text" name="platMap" required>
            <label> Start Date </label> <input type="date" name="startingDate" required>
          </fieldset>
          <input class="btn" type="submit" id="add-project" value="Add a Project">
	  </form> <!--add new project-->
</div>
</body>
</html>